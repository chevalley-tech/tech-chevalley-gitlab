apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitlab-ce-pv-claim
  labels:
    app: gitlab
spec:
  storageClassName: rook-ceph-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi